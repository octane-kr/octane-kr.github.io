{
  "name": "2026may-blog",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "generate:lens": "node scripts/generateLensIndex.mjs",
    "check:posts": "node scripts/checkPublishedPosts.mjs",
    "predev": "npm run check:posts && npm run generate:lens",
    "dev": "astro dev",
    "prebuild": "npm run check:posts && npm run generate:lens",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.105.3",
    "astro": "^6.2.2",
    "katex": "^0.16.45",
    "rehype-katex": "^7.0.1",
    "remark-math": "^6.0.0"
  }
}
